# Enable hardware SHA acceleration for ESP32-S3
# This makes mbedTLS automatically use the ESP32-S3 SHA hardware accelerator
CONFIG_MBEDTLS_HARDWARE_SHA=y

# Enable compiler optimizations for mining performance
# Use "Optimize for performance" instead of default "Debug" optimization
CONFIG_COMPILER_OPTIMIZATION_PERF=y
